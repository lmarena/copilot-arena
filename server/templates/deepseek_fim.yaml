# Since deepseek_fim_client.py makes requests with a prefix/suffix directly, `prompt` and `args` are not utilized.

prompt: |
  {prefix}

stop_tokens:
  - "\n\n"
  - "\n \n"
  - "<｜end▁of▁sentence｜>"
  - "<｜fim▁begin｜>"
  - "<｜fim▁hole｜>"
  - "<｜fim▁end｜>"

args:
  - prefix
